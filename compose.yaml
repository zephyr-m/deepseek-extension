services:
  deepseek-bridge:
    build: .
    environment:
      HOST: 0.0.0.0
      PORT: 8787
      ENABLE_BASH_TOOL: "1"
      BASH_TOOL_SHELL: /bin/bash
    ports:
      - "127.0.0.1:8787:8787"
    restart: unless-stopped
